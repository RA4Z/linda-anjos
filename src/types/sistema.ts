export interface CarrinhoType {
    id: any,
    image: string,
    title: string,
    quantity: number,
    unityValue: number,
    value: number,
}

export interface ItensType {
    id: any,
    title: string,
    image: string,
    category: string,
    information: string,
    unityValue: number,
}